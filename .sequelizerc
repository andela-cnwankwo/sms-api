const path = require('path');

module.exports = {
  "config": path.resolve('./settings', 'config.json'),
  "models-path": path.resolve('./app/models'),
  "seeders-path": path.resolve('./server/seeders'),
  "migrations-path": path.resolve('./server/migrations')
};